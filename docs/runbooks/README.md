# Runbooks

Step 5 (`OPS-05`) deliverable. Placeholder directory so the repository
skeleton (`1.1`) is complete; the actual runbooks are written against real
operational behaviour, which doesn't exist until Step 4/5:

- Our scan appears to have degraded a customer service.
- A scan is stuck.
- Vulnerability data is stale in an air-gapped site.
- Disk is full.
- The appliance is suspected compromised.
- Restore from backup.
